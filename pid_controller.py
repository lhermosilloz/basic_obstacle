#!/usr/bin/env python3

# Foundations for the PID Controller that will be used for our drone

# PID Controller Class
# Should be able to take in parameters for Kp_linear, Ki_linear, Kd_linear, Kp_angular, Ki_angular, Kd_angular,
# error_position, error_heading, error_final_angle, prev_error_position, prev_error_heading,
# integral_position, integral_heading, dt
# Should have methods that take in end goals (x, y, z, heading) and current state (x, y, z, heading)
class PIDController:
    pass